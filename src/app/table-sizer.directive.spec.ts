import { TableSizerDirective } from './table-sizer.directive';

describe('TableSizerDirective', () => {
  it('should create an instance', () => {
    const directive = new TableSizerDirective();
    expect(directive).toBeTruthy();
  });
});
